[build]
  command = "npm run build"
  publish = "dist"

# Optional: set environment variables here when using the Netlify UI or API instead
[build.environment]
# VITE_API_URL = "https://cgjbcsc.onrender.com"

[dev]
  command = "npm run dev"
  port = 5173
