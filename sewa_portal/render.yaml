services:
  # PostgreSQL Database
  - type: pserv
    name: sewa-portal-db
    env: docker
    plan: free
    region: oregon
    ipAllowList: []

  # Web Service
  - type: web
    name: sewa-portal-backend
    env: python
    plan: free
    region: oregon
    buildCommand: "pip install -r requirements.txt && python manage.py migrate --noinput && python manage.py collectstatic --noinput"
    startCommand: "gunicorn sewa_portal.wsgi:application --bind 0.0.0.0:$PORT"
    envVars:
      - key: DJANGO_SECRET_KEY
        value: "replace-with-secret"
      - key: DJANGO_DEBUG
        value: "False"
      - key: DATABASE_URL
        fromDatabase:
          name: sewa-portal-db
          property: connectionString

  # Automatic Database Backup - Runs every 2 days at midnight UTC
  - type: cron
    name: database-backup
    env: python
    plan: free
    region: oregon
    schedule: "0 0 */2 * *"  # Every 2 days at midnight UTC
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python manage.py backup_database --cleanup"
    envVars:
      - key: DATABASE_URL
        fromService:
          name: sewa-portal-backend
          type: web
          property: connectionString
      - key: DROPBOX_APP_KEY
        sync: false
      - key: DROPBOX_APP_SECRET
        sync: false
      - key: DROPBOX_REFRESH_TOKEN
        sync: false
